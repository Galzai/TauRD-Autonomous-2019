#ifndef SENSOR_LOC_H_
#define SENSOR_LOC_H_

/*
 *Set sensor spatial data relative to vehicle's center of gravity.
 */

/*
 *defines for camera spatial data
 */
#define CAM_X_OFFSET 0 // Camera X offset [mm]
#define CAM_Y_OFFSET 0 // Camera Y offset [mm]
#define CAM_Z_OFFSET 0 // Camera Z offset [mm]
#define CAM_PITCH 0 // Camera pitch offset [rad]

/*
 *defines for GPS spatial data
 */
#define GPS_X_OFFSET 0 // GPS X offset [mm]
#define GPS_Y_OFFSET 0 // GPS Y offset [mm]
#define GPS_Z_OFFSET 0 // GPS Z offset [mm]
#define GPS_PITCH 0 // GPS pitch offset [rad]
#define GPS_ROLL 0 // GPS roll offset [rad]
#define GPS_YAW 0 // GPS yaw offset [rad]



#endif /* SENSOR_LOC_H_*/

